import { styles } from '@/app/styles';

const MainSection = () => {
  return (
    <section id="main" className={`${styles.padding} relative min-h-screen text-tertiary flex justify-center items-center`}>
      MainSection
    </section>
  );
};

export default MainSection;
